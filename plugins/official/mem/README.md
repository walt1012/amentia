# Mem Plugin

The official `mem` plugin is the default memory layer for `Cavell`.

Planned responsibilities:

- workspace facts
- reusable project notes
- thread summaries
- lightweight retrieval for prompt assembly

The full implementation will arrive in the plugin milestone. The current scaffold exists so the repository and runtime can treat `mem` as a first-party built-in plugin from day one.
